# Server port (Render/Railway set this automatically)
PORT=3000

# Path for SQLite database file
# On Render: use /data/recipes.db with a persistent disk mounted at /data
# Locally: defaults to ./data/recipes.db
DB_PATH=./data/recipes.db
